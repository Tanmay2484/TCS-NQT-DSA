NQT : Cross bikers make pair of (0,1) 
#include <bits/stdc++.h>
using namespace std;

int main()
{
   int n,count=0,sum=0,c=0;cin>>n;
   int arr[n]={0,1,0,1,1};
   for(int i=0;i<n;i++){
     if(arr[i]) sum+=c;
     else c++;
   }
   cout<<count;
}
